{
  // Client-side compiler shape: browser library API, React JSX, no ambient
  // node types (mirrors the official tsconfig.base.client.json).
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "lib": ["ES2024", "DOM", "DOM.Iterable"],
    "types": []
  }
}
